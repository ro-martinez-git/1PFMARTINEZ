import { ConcatFieldsPipe } from './concat-fields-pipe';

describe('ConcatFieldsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatFieldsPipe();
    expect(pipe).toBeTruthy();
  });
});
